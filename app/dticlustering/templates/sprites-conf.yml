model:
  name: dti_sprites
  n_sprites: 10
  prototype:
    source: data # generator
    data:
      # [foreground, background, masks]
      freeze: [False, False, False]
      value: [0.1, 0.5, 0.]
      init: ["constant", "mean", "gaussian"]
      gaussian_weights_std: 25
      generator: mlp # mlp / unet
